# Contributing to RinGPU

Keep public records size/version prefixed, preserve existing enum values, and
reject malformed input before allocation, submission, or publication. New
hardware-specific behavior belongs in an OS-Core backend, not in this public
tree. Every ABI change requires a golden layout assertion and a host test.
