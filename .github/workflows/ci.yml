name: RinGPU CI

on:
  push:
  pull_request:

jobs:
  host-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -DRINGPU_BUILD_TESTS=ON -DRINGPU_BUILD_DIFFERENTIAL_TESTS=ON
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure
